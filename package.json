{
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/emails-input.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "assets"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "start:lib": "tsdx watch --format umd --transpileOnly ",
    "start:server": "live-server .",
    "start": "concurrently yarn:start:lib yarn:start:server",
    "build": "tsdx build --format umd",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepare": "yarn build"
  },
  "peerDependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "tsdx lint"
    }
  },
  "name": "email-input",
  "author": "Raul",
  "module": "dist/email-input.esm.js",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "husky": "^4.3.0",
    "live-server": "^1.2.1",
    "prettier": "^2.1.1",
    "tsdx": "^0.13.3",
    "tslib": "^2.0.1",
    "typescript": "^4.0.2"
  }
}
